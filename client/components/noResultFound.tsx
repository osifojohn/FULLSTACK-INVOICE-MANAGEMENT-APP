const NoResultFound = ({ text }: { text: string }) => {
  return (
    <div className="font-bodyFont text-center text-xl text-gray-500 my-8">
      {text}
    </div>
  );
};

export default NoResultFound;
